apiVersion: v1
kind: Service
metadata:
  name: orderpulse-api
  namespace: orderpulse
  labels:
    app: orderpulse-api
spec:
  type: ClusterIP
  selector:
    app: orderpulse-api
  ports:
    - port: 80
      targetPort: 8080
      name: http
