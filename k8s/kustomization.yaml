apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: orderpulse
resources:
  - 00-namespace.yaml
  - 01-configmap.yaml
  - 03-storageclass.yaml
  - 04-db-statefulset.yaml
  - 05-api-deployment.yaml
  - 06-api-service.yaml
  - 07-api-hpa.yaml
  - 08-ingress.yaml
# Note: 02-secret.template.yaml is intentionally NOT listed here.
# Run scripts/create-secrets.sh first to create real secrets in-cluster.
